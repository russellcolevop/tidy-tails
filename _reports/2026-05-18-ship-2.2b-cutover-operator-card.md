---
title: Ship 2.2b — cutover operator card
venture: tidy-tails
date: 2026-05-18
status: READY — awaiting Russell's explicit final-approval phrase
type: cutover-operator-card
ship: 2.2b
production-ref: pgkwovokciaqnbhpttba
parent-docs:
  - _reports/2026-05-18-ship-2.2b-production-cutover-runbook.md
  - _reports/2026-05-18-ship-2.2b-production-forward-migration.sql
  - _reports/2026-05-18-ship-2.2b-production-rollback-migration.sql
---

# Ship 2.2b — cutover operator card

One page. The exact commands and checks for cutover day. The full reasoning is
in the runbook (`2026-05-18-ship-2.2b-production-cutover-runbook.md`) — this card
is the thing you keep open while you work.

## ⛔ The production gate

No production SQL runs until **Russell types this exact phrase**:

> **`FINAL APPROVAL: run the Ship 2.2b production RLS cutover now`**

A casual "yes" / "go" / "ship it" is **not** enough. Everything above the apply
step (§4) is read-only or operates on a disposable backup.

---

## 1. Before cutover day — prerequisites

| # | Item | Status |
|---|---|---|
| 1 | Sam's `auth.users` UID captured (`88413167-0799-49a7-ba4c-c1c29403e038`) | ✅ done |
| 2 | Forward + rollback rehearsed green on a Supabase branch | ✅ done |
| 3 | `dump_supabase.py` present + committed in `venture-ops/` | ✅ done |
| 4 | v2 deployed to Vercel Production, env vars set (`USE_LIVE_DATA=on`, prod URL, anon key) | ✅ verified 2026-05-18 |
| 5 | v2 redeployed 2026-05-18 from current source (`dpl_BayckTDSvoex6V69udwLBSnvWp8h`) — includes every surface | ✅ done — see §2 |
| 6 | Supabase Pro / PITR decision made consciously | `VERIFY` — Russell |
| 7 | ~30-min quiet window agreed with Samantha; she logs the day's work in v1 first | `VERIFY` — Russell |

If §5 or any `VERIFY` is unmet → **do not proceed.**

## 2. v2 deployment — current

v2 was redeployed 2026-05-18 from the current committed source (deployment
`dpl_BayckTDSvoex6V69udwLBSnvWp8h`). Production now includes every workflow
surface, and the four write surfaces ship **gated** — the write-gate flags
(`TIDYTAILS_ENABLE_*`) are OFF in every environment.

- Production URL: **https://tidy-tails-v2.vercel.app**
- If any v2 code lands before cutover day, redeploy:

  ```sh
  cd ~/Developer/RussellLabs/tidy-tails/v2
  git -C .. status --short -- v2/      # expect empty (v2 source committed)
  vercel --prod
  ```

- Confirm before cutover: open the URL, sign in as `sammclennan143@gmail.com`,
  the dashboard loads; the four write surfaces still show their gated message.

---

## 3. Cutover-day sequence — commands

### T‑30 — take the final fresh backup

> A verified `service_role` backup was already taken overnight 2026-05-18 →
> `venture-ops/backups/tidy-tails/2026-05-18-cutover/`, readback-clean
> (`2026-05-18-ship-2.2b-final-precutover-check.md` §2). Use it as the baseline
> **only if** the T‑15 drift check below shows production still at
> 137 / 188 / 737 / 0 / 0 / 0 / 69. If it has drifted, take a fresh backup with
> the command below — that needs Russell to re-supply the service_role key.

Run in a terminal (`! <command>` in a Claude session captures the output here):

```sh
cd ~/Developer/RussellLabs/venture-ops
export SUPABASE_URL='https://pgkwovokciaqnbhpttba.supabase.co'
export SUPABASE_KEY='<PRODUCTION service_role key — paste, never save to a file>'
python3 dump_supabase.py --label cutover
unset SUPABASE_KEY
```

- ✅ Pass: exit code `0`; per-table exported-vs-server counts reconcile, no
  mismatch. Output → `venture-ops/backups/tidy-tails/<date>-cutover/` (gitignored
  — never commit it).
- `MANIFEST.json` must record `key_role: service_role`. If it says `anon` →
  reject the backup, re-run.
- **`MANIFEST.json` is THE baseline.** Every count check below compares to it —
  never to a number in any planning doc.

### T‑20 — verify the backup

- [ ] Re-check each dump file's SHA-256 against `MANIFEST.json`.
- [ ] Each table's manifest count == its server `count(*)` (the script asserts
      this — confirm it printed).
- [ ] Spot-check one known client by name in `clients.json`.
- ❌ Any checksum/count mismatch or missing record → **stop. Do not migrate.**

### T‑15 — confirm production has not drifted (read-only)

- [ ] Re-run the row-count SELECTs (`2026-05-18-ship-2.2b-final-precutover-check.md`
      §1 item 9) → confirm `137 / 188 / 737 / 0 / 0 / 0 / 69`. Any drift → take a
      fresh T‑30 backup (Russell must re-supply the service_role key); the new
      `MANIFEST.json` becomes the cutover-green baseline.
- [ ] Re-run the policy/view/advisor snapshot; diff vs `2026-05-17-prod-preflight-policies.json`
      / `-advisors.json`. Expect 21 permissive policies, `client_overview`
      present, 19 security lints. Any drift → **stop** (drift invalidates the
      rollback SQL).
- [ ] Re-run the UID query → still `88413167-0799-49a7-ba4c-c1c29403e038`.
- [ ] The forward SQL's embedded UID matches.

### T‑10 — confirm Sam can log into v2

- [ ] Open https://tidy-tails-v2.vercel.app, sign in as `sammclennan143@gmail.com`.
- [ ] Dashboard, client list, a client detail, reports all render with live data.
- ❌ Login fails or no data → **stop.** Do not take v1 dark.

### T‑0 — APPLY  ⛔ REQUIRES THE FINAL-APPROVAL PHRASE

> Apply only after Russell types: **`FINAL APPROVAL: run the Ship 2.2b
> production RLS cutover now`**. At COMMIT, v1 goes dark — v2 is the live tool.

Apply `2026-05-18-ship-2.2b-production-forward-migration.sql` as **one named
migration**. Pick one mechanism:

- **MCP path:** `apply_migration(project_id='pgkwovokciaqnbhpttba', name='ship_2_2b_forward_rls_hardening', query=<full forward SQL file>)`
- **Dashboard path:** paste the full file into the SQL Editor for
  `pgkwovokciaqnbhpttba`, wrapped in `BEGIN; … COMMIT;`.

Runs in seconds. The file ends with `NOTIFY pgrst, 'reload schema';`.

### T+0 — verify (read-only)

Run the §2.6 verification query from the runbook against `pgkwovokciaqnbhpttba`,
then the REST + authenticated + gated-writes checks.

---

## 4. "Cutover green" — all 8 must hold

1. Row counts unchanged vs `MANIFEST.json` (6 in-scope tables + `sam_review_responses`).
2. `groomer_id` on all 6 tables; `is_nullable=NO`; default `auth.uid()`; every
   row's `groomer_id` = `88413167-0799-49a7-ba4c-c1c29403e038`, zero nulls.
3. `permissive_survivors` = `[]`; `groomer_policy_count` = `24`.
4. `client_overview` dropped (`client_overview_exists` = `0`).
5. Anon REST `GET` clients/pets/appointments → `200 []`; anon `POST` → `401/403`.
6. v2 authenticated reads work — Sam signs in; lists/details/reports render.
7. `get_advisors(security)` — in-scope permissive + SECURITY DEFINER lints cleared.
8. All 4 v2 write surfaces still return their **gated** result (a test entry in
   each persists no row).

All 8 → **green**: proceed to the watch week. Any of 1–7 fails → **§5 rollback**.
Criterion 8 failing is an app-config fix, **not** a DB rollback.

---

## 5. Rollback — command

Roll back if any §4 criterion 1–7 fails, or a watch-week trigger fires (runbook
§4.1). Apply `2026-05-18-ship-2.2b-production-rollback-migration.sql` as one
named migration:

```
apply_migration(project_id='pgkwovokciaqnbhpttba', name='ship_2_2b_rollback_rls_hardening', query=<full rollback SQL file>)
```

(or paste into the dashboard SQL Editor wrapped in `BEGIN; … COMMIT;`.)

Then verify (read-only): row counts still match `MANIFEST.json` (rollback drops
a *column*, never a *row*); `groomer_id` absent; 21 permissive policies +
`client_overview` restored; anon REST `GET` returns rows again; advisors back to
the 19-lint set. v1 is a working data client again within minutes.

Second safety net: if the rollback migration cannot be applied, restore from the
T‑30 service_role backup (`restore_supabase.py` — a separately-approved action).

---

## 6. After green — writes stay gated

The migration makes writes **safe**, not **active**. All 4 write surfaces stay
gated. No SMS. Surfaces flip on **one at a time across the watch week**, each its
own approval + mini-verifier — see `2026-05-18-ship-2.2b-write-flip-plan.md` and
`2026-05-18-ship-2.2b-write-flips-1-2-code-prep.md`. Do not flip anything on
cutover day.

---

*Generated 2026-05-18. This card does not authorize the cutover — the §3 T‑0
apply is gated on Russell's exact final-approval phrase. Production
`pgkwovokciaqnbhpttba` has not been mutated.*
