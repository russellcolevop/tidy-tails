# Handoff — tidy-tails

Single source of truth for "whose turn is it, and what are they doing?" on Tidy Tails. Every agent (Russell, Cowork, Claude on the VPS) edits this file before stopping. Committed to git so the history becomes the project's working log.

## Current state

- **Whose turn**: Russell — pick the next lane: v2 M2 write foundation (logging + inline-edit — the prerequisite for the remaining M3 increment, booking from the identified household), v2 Ship 2.2b (RLS flag-day cutover) prerequisites, or a data lane (Korrie/Gavi reconciliation, Landry/Laundry patch, contact-card batches; remaining Kiwi 2024-07 day is the only unresolved Phase-3.5.1 candidate). Reminder work is parked for a later slice — see the reminder product rule under "Context a fresh agent needs."
- **Focus**: v2 M3 wedge — first slice landed in `v2/` and **browser-verified 2026-05-17**. The Call/Text → Identify → Book search is rebuilt: one box matches owner first/last name, phone, or pet name with partial + typo-tolerant (fuzzy) matching and ranked results; each result is a household card showing owner, phone, every pet (with allergy flags), last visit, and per-pet usual service / usual price; a shared pet name (two "Bella"s) is disambiguated by emphasizing the matched pet. The pure search + ranking + derivation logic is unit-tested (Vitest — the venture's first test harness — 39 tests). Read-only — no booking writes, no SQL, no RLS change; fixture mode stays default; Ship 2.2a auth still gates every route. Prior context: Ship 2.2a auth landed; **Phase 3.5.1 appointment backfills committed 2026-05-17 — live state now 137 clients / 188 pets / 737 appointments (was 735 pre-3.5.1).** Two date-ambiguous rows shipped (Ruby 2024-08-28 + Coco 2023-12-14); only Kiwi 2024-07-?? remains, pending Russell's own readback.
- **Reason**: Russell chose to build the M3 wedge with Vitest + TDD for the pure logic and browser testing for the UI. typecheck + lint + production build + 39 unit tests all green. The interactive authenticated search click-through is now verified — Russell ran the browser smoke test 2026-05-17 and all 7 checks passed (see the M3 verification entry under "What just happened"). The M3 first slice is **verified, committed (`0178e50`), done.**
- **Updated**: 2026-05-17 (CC — M3 fast-lookup slice browser-verified)
- **Updated by**: CC

## Sam's review pipeline — operational reference

- **Public URL** (share with Sam): `https://symphonious-starlight-2a7ecc.netlify.app`
- **HTML source**: `_reports/sam-review.html` (re-deploy by drag-drop to Netlify Deploys tab)
- **Supabase table**: `public.sam_review_responses` (INSERT-only RLS for anon; SELECT via service_role only)
- **Notify edge function**: `notify-sam-review-complete` (uses Resend; `RESEND_API_KEY` in function secrets)
- **Watch live**: `https://supabase.com/dashboard/project/pgkwovokciaqnbhpttba/editor` → `sam_review_responses` table, filter by latest session_id
- **Sam's first-batch answers**: captured in `_reports/2026-05-13-sam-answers-batch-1.md`
- **Sam's session_id (first batch)**: `ddc94583-05c2-4d52-b262-048ec9d7e496`
- **Cleanup tech debt (low priority)**: dead `sam-review` storage bucket + `sam-review` edge function left from a failed hosting attempt; harmless but messy

## What just happened

- **v2 M3 wedge — first slice browser-verified** (2026-05-17, Russell). Russell ran the authenticated browser smoke test for the M3 fast-lookup search wedge and **all 7 checks passed**: (1) empty search shows all 22 households alphabetized by last name; (2) phone fragment `0118` returns exactly Maya Albright / Biscuit; (3) `Park` returns the two Park households; (4) `Bella` returns Glen Okafor + Marisol Park with each matched Bella emphasized; (5) `biscut` fuzzy-matches Maya Albright / Biscuit; (6) the allergy chip is correct — Glen Okafor's Bella shows Allergy, Marisol Park's Bella does not; (7) auth route protection holds — unauthenticated `/` redirects to `/login`, `/login` serves normally. Re-ran verification: `npm test` 39 passed, `npm run typecheck` pass, `npm run lint` pass. The M3 first slice (commit `0178e50`) is now **verified and done** — *identify* only; the booking write path is the remaining M3 increment, gated on M2. No code change this entry — verification only.
- **v2 M3 wedge — fast-lookup search, first slice** (2026-05-17, CC). Built the first slice of the Call/Text → Identify → Book wedge (PRD §1.1, M3) in the `v2/` app. Search is rebuilt: one box matches across owner first/last name, phone digits, and pet names, with exact / prefix / substring / **fuzzy** matching (Levenshtein, typo tolerance scaled to token length) and **ranked** results — exact beats fuzzy, prefix beats substring. Each result is a **household card** — owner, phone, every pet with its allergy flag, the household's last visit, and per-pet **usual service** and **usual price** (derived from appointment history on read; no stored columns, per PRD §6). When a pet name is shared across households — two dogs named "Bella" — the card emphasizes the matched pet so Sam sees *which* Bella at a glance (disambiguation, PRD §1.1). New pure module `v2/lib/search.ts` (`searchHouseholds`) and additions to `v2/lib/derive.ts` (`usualService` = mode, `usualPrice` = median) are **unit-tested with Vitest** — the venture's first test harness — 39 tests across `search.test.ts` / `derive.test.ts`, written test-first (TDD: watched fail, then implemented). UI: `v2/app/(app)/page.tsx` and `v2/components/ClientSearch.tsx` rewritten, new `v2/components/HouseholdCard.tsx`; `v2/lib/data/fixtures.ts` gains 2 synthetic clients / 3 pets so two households each hold a "Bella" (plus a shared "Park" surname) — making disambiguation real for the browser test. Verification: typecheck + lint + production build + 39 tests all green; a dev-server curl check confirms the proxy still 307-gates every app route after the rewrite. The interactive authenticated search click-through (typing queries, seeing cards, the two-"Bella" disambiguation) was handed to Russell, who browser-verified it 2026-05-17 — all 7 smoke-test checks green (see the verification entry above). Read-only: **no booking / appointment writes** (deferred — this slice *identifies*; creating an appointment is the next M3 increment, gated on the M2 write path), no SQL, no RLS/schema change, no SMS; fixture mode stays the default; Ship 2.2a auth is untouched and still gating every route. Two known notes: "usual duration" is omitted from the cards — the data model has no per-appointment duration yet (PRD §6.2, gated on Q-B1); and the live-data path's `repo.ts` maps appointment `service`/`price`, while the live schema uses `service_type`/`fee` — a pre-existing Ship 2.1 mapping gap that would blank out usual service/price *if* live mode were enabled (live mode is off by default; fixtures are unaffected). Committed: the `v2/` M3 files and this HANDOFF.
- **v2 Ship 2.2a — real Supabase Auth implemented** (2026-05-17, CC). Replaced the Ship 2.1 placeholder cookie gate with real Supabase Auth in the `v2/` app. App-side auth only — Russell scoped it explicitly as "no database migrations, no RLS change": the live DB stays on its current permissive policies until the Ship 2.2b flag-day cutover. New files: `v2/lib/supabase/{env,server,client,proxy}.ts` — a server/client Supabase pair for the App Router plus the proxy session-refresh helper — and `v2/components/LoginForm.tsx`, a client form with inline error + pending state via `useActionState`. Rewritten: `v2/proxy.ts` (delegates to the Supabase session updater — gates every app route, refreshes the SSR session, redirects unauthenticated → `/login` and signed-in-on-`/login` → `/`); `v2/lib/actions/auth.ts` (`signIn` via `signInWithPassword` with operator-friendly error mapping, `signOut` via `auth.signOut()`); the login page (renders the real form, placeholder notice removed); the settings page (Account card shows the signed-in email, marked `force-dynamic` since it reads per-request auth). Deleted `v2/lib/session.ts` (the placeholder). Verification: typecheck + lint + production build all green; against the dev server every app route (`/`, `/settings`, `/clients/*`, `/reports`) 307-redirects unauthenticated to `/login`, `/login` serves 200 with the real form, and the Supabase token endpoint + anon key resolve with the expected `Invalid login credentials` contract. The interactive sign-in → app → sign-out click-through needs a browser + a Supabase Auth account and is handed to Russell. Note: `@supabase/ssr` is v0.10.3 — its `setAll` cookie callback takes a second `headers` argument carrying no-store cache directives, and the proxy applies both (caught by reading the installed package per `v2/AGENTS.md`). No DB migration, no SQL, no RLS/schema change, no SMS, no live writes — data access stays read-only and fixtures stay the default. Committed: the `v2/` Ship 2.2a files and this HANDOFF.
- **Sam Scheduling OS PRD — immediate wedge tightened** (2026-05-16, CC). Russell relayed a follow-up product correction from himself and Samantha: keep v2 as the scheduling source of truth, but tighten the *immediate* build wedge. The first product win is **not** complex scheduling automation — it is **fast client/pet lookup the moment someone calls or texts.** Samantha wants to type a name, phone number, pet name, or partial clue and instantly pull the right household — which "Bella," which owner, the phone, the household's pets, last visit, usual service, usual price/duration, allergies/notes — enough to book confidently without digging through cards or re-asking a repeat client. Scheduling *judgment* is not her hard problem; retrieval/context is. Planning docs updated (no code, no SQL, no Supabase mutation): the PRD gains §1.1 (the wedge) and is re-sequenced — **M3 is now the fast-lookup / disambiguation / confident-booking wedge** (search by phone / owner first-or-last name / pet name, fuzzy + partial matching, common-pet-name disambiguation, household cards, booking from the identified household); **M4** is the today + upcoming schedule-of-record view + recurring availability; **M5** guided warnings stay but are explicitly secondary and advisory. Two new §10 acceptance criteria: identify a repeat client/pet in under 10 seconds; book without re-asking details already in the app. §7 Flow 2 rewritten as "Call/text → identify the household → book confidently." The source-of-truth + wedge scope change is logged in `docs/DECISIONS.md` as a combined 2026-05-16 entry (this also closes the previously-flagged DECISIONS.md gap). The answers artifact gains a "Follow-up correction" section; the KoyaOS dogfood doc gains REQ-26 (operator discovery must separate the job-to-be-done from adjacent complexity). Committed: the PRD, the answers artifact, the KoyaOS doc, `docs/DECISIONS.md`, this HANDOFF.
- **Sam Scheduling OS PRD updated from Samantha's Group A answers** (2026-05-16, CC). Samantha answered all six Group A questions (relayed by Russell). The headline answer (Q-A4): Tidy Tails *should be the real source of truth for upcoming appointments,* not a mirror of paper/phone. This inverts the prior "scheduling-aware cockpit" stance. The PRD was rewritten end-to-end: §1 now states the scope change plainly (a **deliberate** change beyond what the locked recon doc imagined — it scoped a conservative "today list"; Sam asked for source-of-truth ownership — so a `docs/DECISIONS.md` entry is recommended); §4 redefines v2 as a **phased guided scheduler** (M3–M4 spine = today/upcoming schedule + manual appointment entry; M5 = soft, explained warnings around capacity/large-dog runs/crate space/duration/temperament/location; M6 = reminders/rebooking) governed by a "do not over-automate Samantha's judgment" principle (warn and explain, never silently block, except genuine impossibilities like a non-Tidy-Tails day). The retired graduation gate is gone. §3 carries her recurring weekly pattern (Mon/Thu + alternating Fri = Tidy Tails grooming; Tue = Gina; Wed = Annette; alternating Fri = Annette; one Sat/month nail clinic at Ren's) and her contextual capacity rules (avoid >3 large dogs in a row; Annette's crates hold 2 big dogs; ~6 dogs/day, all judgment-dependent). §6.1 defines a scheduling-rules model (recurring availability with alternation + Nth-weekday-of-month, a flexible location model so Ren's is data not a schema change, crate/large-dog/daily capacity as soft references, service duration estimate + per-dog override for dogs like Milo/Chloe, temperament override, "can be out of crate" flag, hard vs soft warnings). §8 was reframed from "fix bad records" to **"Keeping the digital book current"** — Sam does not see her cards as wrong; the reconciliation digitized a working paper system. Residual follow-ups R1–R4 (service list, daily hours, annette/gina row meaning, reminder dispatch-vs-surface) remain but do not block M1–M3. Sam's answers captured in a new artifact `_reports/2026-05-16-sam-scheduling-answers.md`. KoyaOS dogfood doc gained REQ-24 (pair a deferral gate with simply asking the operator — the graduation gate collapsed the moment Sam was asked plainly) and REQ-25 (elicit operator-facing language, don't impose it — Sam rejected the builder's "fix bad records" frame). No code, no SQL, no Supabase mutation. Committed: the PRD, the answers artifact, the KoyaOS doc, this HANDOFF.
- **Sam Scheduling OS PRD written** (2026-05-16, CC). `_reports/2026-05-16-sam-scheduling-os-prd.md` — defines what a working v2 app for Samantha is beyond the read-only scaffold. Resolved a premise conflict: the kickoff named the workstream "Scheduling OS" but four locked docs forbid building a calendar. Russell chose Option A — v2 is a scheduling-*aware* cockpit, not a scheduler, with a "Not Yet: Full Scheduler" boundary and a graduation decision gate (criteria + 90-day review cadence + owner). The PRD carries a gap analysis (v1 vs scaffold vs working app), a 9-part scheduling domain model with every parameter tagged KNOWN or UNKNOWN (no fabricated operator data — a grep of Sam's batch-1 answers confirmed zero scheduling data exists), a question batch for Sam (Group A must-answer-before-build / Group B learn-during-watch-week), schema implications (described, no migrations), 6 UX flows, a 7-milestone plan, and acceptance criteria including a lived-adoption bar. The live `public` schema was inspected read-only (`list_tables`) to ground the gap analysis — it revealed v1 is Samantha's working Supabase-backed operator app (not a marketing site) and that the schema already carries `appointments` status/service/location enums, structured `pets` fields, and a `clients.preferred_frequency_weeks` cadence column, so most cockpit writes need no migration. A 2026-05-16 checkpoint pass then added **§8 — "Sam can fix the book herself,"** making data correction a first-class workflow (inline editing from every detail surface, add-missing-client/pet/appointment flows, archive instead of hard delete, tiered confirmations for high-risk changes, an audit/edit-history requirement, "unknown for now" field handling, single- and future-multi-groomer role/auth assumptions, and its own acceptance criteria) — plus §12 PRD review notes. A plain-language Samantha-facing question sheet for §5 Group A was created at `_reports/2026-05-16-sam-scheduling-questions.md`. No code, no Supabase mutation (schema read read-only only). The PRD, the KoyaOS dogfood doc, the question sheet, and this HANDOFF were committed 2026-05-16. KoyaOS dogfood lessons REQ-19–23 at `_reports/2026-05-16-koyaos-dogfood-sam-scheduling-os-prd.md`.
- **v2 Ship 2.2 auth + RLS plan written** (2026-05-15, CC). Planning-only artifact at `_reports/2026-05-15-v2-ship-2.2-auth-rls-plan.md`: flag-day cutover (no anon bridge), app-side Supabase Auth wiring (file-by-file), the RLS-hardening migration as draft SQL (`groomer_id` + `auth.uid()`-scoped policies on all six tables + drop `client_overview`), rollback plan, backup requirement, and a verifier checklist. Nothing executed — no SQL run, no migration applied, no backup taken. KoyaOS dogfood lessons REQ-15–18 at `_reports/2026-05-15-koyaos-dogfood-v2-ship-2.2-planning.md`.
- **v2 Ship 2.1 scaffold committed** (2026-05-15, CC, `2e9a5cb`). 57 files — `v2/` plus the venture docs (`CONTEXT.md`, `ROADMAP.md`, `docs/DECISIONS.md`, the scaffold + KoyaOS reports). `docs/home.html` was deliberately excluded: it carries an unrelated uncommitted v1 heading-color change (purple `#9B30FF` → pink `#FF69B4`) for Russell to confirm or `git checkout` separately.
- **v2 Ship 2.1 read-only scaffold built** (2026-05-15, CC). A production-quality, mobile-first, installable PWA grooming cockpit in `v2/` — Next.js 16 (App Router) + TypeScript + Tailwind v4. Surfaces: search-first home, client detail, allergy-first pet safety cards, pet detail with vaccination status, appointment history, lapsed clients, revenue by month, tiny settings. Groom-logging and SMS-reminder flows are built as UI but write-disabled behind a gate notice. Placeholder auth (cookie gate via `proxy.ts`); real Supabase Auth deferred to Ship 2.2. Anonymized synthetic fixtures are the default data source; a live `SELECT`-only Supabase read is available behind `NEXT_PUBLIC_USE_LIVE_DATA`. PWA baseline shipped: manifest, placeholder icons, service worker, installability. Build clean, browser-verified. Repo strategy decided (`v2/` subdir, same repo) and logged in `docs/DECISIONS.md`. Scaffold addendum at `_reports/2026-05-15-v2-ship-2.1-scaffold-addendum.md`. KoyaOS dogfood lessons at `_reports/2026-05-15-koyaos-dogfood-v2-scaffold.md`.
- **Workstream B — 79 contact cards processed across 8 batches** (2026-05-13). Full report at `_reports/2026-05-13-card-batch-1.md`. Open questions tracked at `_reports/2026-05-13-open-questions.md` (~215 decisions for Russell to triage). Zero DB writes executed — all gated on per-row confirmation per kickoff rule.
- **Findings summary across the 79 cards:**
  - **~60 matches** to existing Supabase clients (with various dedup patterns)
  - **9 truly-new** cards (no match anywhere): cards 2 (Acca/Whiskers), 13 (Caughn/Riley), 26 (Gandy/Coco), 38 (McDoe/Riva), 48 (Ashley Nichols/Khaleesi), 50+51 (Mary Nichols/Megan+Vader as one new household), 62 (Snider/Boone), 70 (Sillman/Loki), 78 (Ethan/Koda)
  - **Excel-only-not-in-Supabase**: 1 (card 36 Lahay/Rocky)
  - **Recurring dedup patterns identified**:
    - 2x to 4x import-double-load on 2026-04-09 21:26-21:28 (universal)
    - First-name-as-last-name in `clients.first_name` column (universal convention)
    - First-name spelling fork (Cory/Kori Handy, Peggy/Lund, Scotland/Nadine Scotland) — 3+ confirmed cases
    - Ghost-pet breed fork (Stella Markie/Doodle, Ziggy Westie/?, Stilly yorkie/large Doodle) — 3+ confirmed cases
    - Truncated surname pattern (Mc + Colleen Louise McKee) — 1+ case
    - Phone typos (795 area code on Backway live row, multiple one-digit-off cases)
    - 2026 visits on cards not in Excel (Excel ends Dec 2025) — recurring gap
  - **Multi-pet households mapped**: Andrews (Bobbi + Wilson), Backway (Ozzy + OJ), Beasley (Ziggy + Kaila), Cooper (Lexie + Theo), Donaldson 705 (Baron + Moxie + Piper + maybe Trigger), Michael Donaldson 437 (Trigs + Moxie), Elford (Baxter + Floyd + Phil), Handy (Ricky + Morty), Hinds-Matt (Zoey), Patterson (Jedi + Socha), Santos (Miles + Harlow), Santocono (Daisy + Oddy + maybe Coby new), Marco (Grizzly + Bently + Grizlik), Mary Nichols-new (Megan + Vader), Mitchell/MacNeil (Reese + Rolo-new), Cooper (Cool dog), Wainman (Loki + Mylla)
- **Pesard/Madden/Pesaro disambiguation logged** (2026-05-13): 705-323-3351 = Lisa Madden (69 visits, $4,550, Cavachons Milo + Chloe); 416-520-5297 = Joanna Pesaro (13 visits, $1,525, Beau Portuguese Water Dog). Excel index updated with `_canonical_identity` notes.
- **You discovered yourself** (Russell Cole) in the customer base with dog Kiwi (Cavachon/poodle). Supabase has client row but ZERO appointments despite Excel showing 1 visit (2024-06-06 $60). Card has 4 visits. Suggest manual backfill for your own account.
- **Workstream A closed (2026-05-13).** Canonical BUILD-venture doc set populated for Tidy Tails:
  - `koya_inputs/00_venture_brief.md` (created)
  - `CONTEXT.md` (populated from stub)
  - `ROADMAP.md` (populated from skeleton; Now / Next / Later / Done all real)
  - `GLOSSARY.md` (created)
  - `METRICS.md` (created, with caveat that MODES.md doesn't formally list it)
  - `v1-final-state-spec.md` (created; Phase 0 to Phase 7 plan; v1 naming collision documented inline)
  - `v1-active-bugs.md` (created; R-1 as S1, `client_overview` SECURITY DEFINER as S2, no automated backups as S2)
  - `docs/DECISIONS.md` (two decisions logged: adopt canonical BUILD doc set; v1 naming collision resolution)
  - `_reports/2026-05-13-koya-milestones.md` (paste-ready milestone list for MC ingest)
  - `_reports/2026-05-13-koya-feedback.md` (12 observations for parallel KoyaOS dev session)
- **Koya feedback relayed.** Russell forwarded the 12 observations into the parallel KoyaOS session. Outcome:
  - All 12 triaged into `koya/v1-active-bugs.md`
  - 3 became Phase 5B ships in the Koya PRD: `koya_inputs/` ingest path (5B.7), doc-parity endpoint (5B.8); mode-transition-single-action moved to Post-v1
  - 3 captured as Post-v1 Roadmap platform-level patterns in `koya/v1-final-state-spec.md`: reconciliation skill, competitive-recon templateable artifact, anchors-as-frontmatter audit tooling
  - 4 small doc edits queued as a Phase 5 doc-cleanup pass: METRICS.md addition to MODES.md, `koya_inputs/`+`_reports/` formalization, Credential Paste Protocol consolidation, CC-kickoff-prompt artifact codification
  - 1 Russell decision pending (see Open decisions): v1 naming collision convention. Cowork recommends keeping `v1-final-state-spec.md` and adding a Naming Clarification template to MODES.md, which mirrors what shipped in Tidy Tails this session.
- **Workstream B starting now.** Russell about to upload the first card batch. Output destination: `_reports/2026-05-13-card-batch-1.md` (or matching dated filename per batch).
- **Workstream C complete** (2026-05-15). Codex performed a read-only logged-in Pawfinity recon. Artifacts:
  - `_reports/2026-05-15-pawfinity-logged-in-recon.md`
  - `_reports/2026-05-15-pawfinity-v2-implications.md`

## What's next

1. **Russell**: mirror the Phase 2 and Phase 3 pre-commit backups off the workspace. Both live under `_private/backups/` (2026-05-16-phase-2-precommit + 2026-05-17-phase-3-precommit, six tables each, MANIFEST + SHA-256). Copy to `~/venture-ops/backups/tidy-tails/` on the Mac so the canonical store stays canonical.
2. **Russell**: ask Sam — Korrie Silver / Gavi reconciliation (3 sub-questions): (a) canonical phone format `647-300-7952` vs `1-647-300-7952`; (b) the two existing Gavi pet rows (Medium Mix + Yorkie Mix) — same dog with mis-extracted breeds, or two different pets?; (c) allergy detail (no chicken, no grain) applies to which Gavi row? Resolution unblocks a small UPDATE patch.
3. **Russell**: ask Sam — Landry/Laundry (705-796-0620): Cash or Charlotte? Same dog or two? Needed for the follow-up patch on that 4-row cluster (still excluded; v1-active-bugs.md B4 stays partially open until this and the Korrie patch land).
4. **Russell**: backfill your own (Russell Cole / Kiwi) appointments — Supabase has the client row but zero appts despite ledger showing 1 visit (2024-06-06, $60) and card showing 4 visits. Now folded into Phase 3.5 planning.
5. **Queued (now unblocked)**: Phase 3.5.1 date-ambiguous appointment backfills. Phase 3.5 committed 5 strong-evidence rows 2026-05-17 14:43 UTC. The 3 deferred rows (Kiwi 2024-07-??, Ruby 2024-08-??, Coco 2023-12-14 year ambiguity) need Sam to read back the original cards before they can ship as a small follow-up patch.
7. **Queued**: Phase 4 Codex enrichment (Typical_Fee, Color, Sex, Special_Notes) after Phase 3.5.
8. **Russell**: review the v2 scaffold and the Ship 2.2 auth + RLS plan (`_reports/2026-05-15-v2-ship-2.2-auth-rls-plan.md`). Since Ship 2.2a, running v2 needs `v2/.env.local` populated first — `NEXT_PUBLIC_SUPABASE_URL` + `NEXT_PUBLIC_SUPABASE_ANON_KEY` (see `v2/.env.local.example`) — and a Supabase Auth account to sign in; the app hard-fails per request without those env vars. Run with `cd v2 && npm install && npm run dev`. Answer design-lock open questions Q2–Q6 (vaccination scope, lapsed threshold, SMS template, v2 URL, parallel-run length) — they gate Ship 2.4/2.5/2.6.
9. **Queued**: Ship 2.2b — the RLS flag-day cutover. Plan: `_reports/2026-05-15-v2-ship-2.2-auth-rls-plan.md`. Ship 2.2a (app-side Supabase Auth) landed 2026-05-17. 2.2b is still hard-gated on the plan's prerequisites: Samantha's `auth.users` account, a written/ratified backup procedure (`dump_supabase.py` does not exist), v2 feature-complete, a rehearsed rollback migration, and a fresh backup. **Follow-up coupled to 2.2b**: the live-data path (`v2/lib/data/repo.ts` + `v2/lib/data/supabase.ts`) still uses the standalone read-only anon client (`getReadOnlySupabase()`), not the auth-aware session client. That is inert under today's permissive RLS, but it MUST switch to the authenticated server client before the 2.2b cutover, or live reads return nothing once policies are scoped to `auth.uid()`. No v2 write path until cutover.
10. **Queued**: more contact-card batches (cards 80–268+ not yet processed).
11. **Russell / Sam — clear residual follow-ups R1–R4.** Group A is answered (2026-05-16); answers captured in `_reports/2026-05-16-sam-scheduling-answers.md` and folded into the PRD. Four small residuals remain (PRD §5 / §11): R1 service list (confirm the four service types are complete/current + what `other` covers), R2 daily hours (Sam's typical start/end on a grooming day), R3 annette/gina row meaning (do those DB rows record Sam's grooming or her employee work for Annette/Gina), R4 reminder dispatch-vs-surface (auto-send vs surface a list Sam sends — default assumed: surface). None block starting the build; they attach to M4–M6. **Build guardrail lifted** — M1 (operator auth) is buildable now.
12. **Russell — continue the v2 build.** The v2 scope change and the fast-lookup wedge are logged in `docs/DECISIONS.md` (combined 2026-05-16 entry). PRD §9 build order: **M1 operator auth ✓** (Ship 2.2a) → M2 logging + inline-edit write foundation → **M3 fast-lookup / disambiguation / booking wedge — first slice landed + browser-verified 2026-05-17 ✓** (search by name/phone/pet with partial + fuzzy matching, ranked household cards, two-"Bella" disambiguation; read-only — *identify* only). The remaining M3 increment is **booking from the identified household** — creating an appointment — which is gated on the M2 write path (Ship 2.2a auth is in place; the write path itself is not built). → M4 schedule-of-record view + recurring availability → M5 guided warnings (secondary) → M6 reminders → M7 RLS cutover. **Reminder dispatch rule** (for M6): the app may prepare reminder texts but must never auto-send — Sam reviews and confirms each SMS in-app before dispatch.

## Next-session opener (paste-ready)

> Picking up Tidy Tails. Read `HANDOFF.md`, the Sam Scheduling OS PRD (`_reports/2026-05-16-sam-scheduling-os-prd.md` — the v2 build spec), and `_reports/2026-05-15-v2-ship-2.2-auth-rls-plan.md`. The v2 app lives in `v2/` (Next.js 16 PWA cockpit). To run it: populate `v2/.env.local` from `.env.local.example` (Supabase URL + anon key), then `cd v2 && npm install && npm run dev`; `npm test` runs the Vitest unit suite. v2 has real Supabase Auth (Ship 2.2a) and the first slice of the M3 fast-lookup search wedge; it is still **read-only** — no booking/appointment writes, fixtures default, live Supabase SELECT-only behind a flag. Next v2 work: the M3 booking increment (needs the M2 write path) or Ship 2.2b (the RLS flag-day cutover, hard-gated on its plan's prerequisites). Data lanes: Phase 3.5.1 date-ambiguous backfills, Korrie/Gavi reconciliation, Landry/Laundry patch. No DB writes, no SQL, no SMS, no RLS migration without Russell's approval.

## Open decisions

- **v1 naming collision (Russell-only).** Cowork recommends keeping `v1-final-state-spec.md` as the canonical Koya filename and adding a Naming Clarification template to `.koya/MODES.md` for any future BUILD venture with a shipped MVP. This mirrors what shipped in Tidy Tails today (see "Naming clarification" section at the top of `v1-final-state-spec.md`). Pending Russell signoff in the parallel Koya session.
- **R-1 fix path — RESOLVED 2026-05-15.** v2 Ship 2.2 closes R-1 with `auth.uid()`-scoped RLS policies plus a `groomer_id` FK (design-lock spec §3.3). Migration sequencing decided: flag-day cutover, no anon bridge. Migration scope ratified by Russell 2026-05-16: security-only (`groomer_id` + RLS + drop `client_overview`); feature schema ships as separate migrations. All logged in `docs/DECISIONS.md`.
- **Repo strategy for v2.0 — RESOLVED 2026-05-15.** v2 lives in the `v2/` subdirectory of the same repo. Logged in `docs/DECISIONS.md`.
- **Commit `v2/`? — RESOLVED 2026-05-15.** Russell approved; the scaffold was committed as `2e9a5cb` (`Ship Tidy Tails v2 read-only scaffold`). `docs/home.html` was deliberately excluded — it carries an unrelated uncommitted v1 heading-color change (purple → pink) for Russell to confirm or revert separately.
- **Pricing model if licensed (Russell-only, deferred to LICENSEABLE_READY).** Free for Samantha forever vs paid tier for other groomers. Candidate $19-$29/mo flat from the Pawfinity recon. Not committed.

## Blockers (external)

- None.

## Context a fresh agent needs

- Read order: `CLAUDE.md` → `HANDOFF.md` (this file) → `CONTEXT.md` → `koya_inputs/00_venture_brief.md` → `v1-final-state-spec.md` → `v1-active-bugs.md`.
- Tidy Tails is a BUILD-mode venture per `.koya/MODES.md`. Sole design partner is Samantha. v1.0 is live and in daily use; v2.0 is planned and not started.
- R-1 risk (permissive RLS) is the largest open issue. Do not write to the live DB without Russell's per-row confirmation.
- Customer data is real. Do not commit data, credentials, or screenshots that include them.
- Workstreams: A (closed), B (active, contact-card batches/reconciliation phases), C (complete, Pawfinity logged-in recon).
- **Reminder product rule (hard, for the M6 reminders slice).** The app may *prepare* reminder texts — draft, queue, surface them — but must **never send automatically**. Sam reviews and confirms each SMS dispatch in-app before it goes out. No code path may call the `send-sms` edge function (or any dispatch) without an explicit in-app confirmation by Sam; a "send all" or scheduled auto-dispatch is out of scope unless Russell re-authorizes it.

## Recent handoff log

| When | From → To | Note |
|---|---|---|
| 2026-05-17 | Russell → CC | **v2 M3 fast-lookup wedge — first slice browser-verified.** Russell ran the authenticated browser smoke test; all 7 checks passed (empty search → 22 households alphabetized; phone `0118` → Maya Albright / Biscuit; `Park` → both Park households; `Bella` → both Bella households, matched pet emphasized; `biscut` → fuzzy-matches Biscuit; allergy chip correct on Glen Okafor's Bella, absent on Marisol Park's Bella; auth route protection holds). Re-verification: `npm test` 39 pass, typecheck pass, lint pass. M3 first slice (commit `0178e50`) is verified and done — *identify* only; booking is the next M3 increment, gated on M2. Reminder work parked for the M6 slice; reminder rule recorded (prepare, never auto-send — Sam confirms in-app). Next: Russell picks the next lane (M2 write foundation, Ship 2.2b prereqs, or a data lane). |
| 2026-05-17 | Cowork → Russell | **Phase 3.5.1 appointment backfills COMMITTED.** Two date-ambiguous rows resolved by Sam's 2026-05-17 follow-up answers: Ruby `2024-08-28` + Coco `2023-12-14`. Fresh pre-commit backup at `_private/backups/2026-05-17-phase-3.5.1-precommit/` (six tables, MANIFEST + SHA-256). All 8 pre-write gates and 7 post-write invariants passed. Final summary: `clients_delta=0`, `pets_delta=0`, `appointments_delta=+2`. Independent post-COMMIT verification at 15:30:09 UTC: 137 / 188 / 737, Landry 4 untouched, Korrie holdout 1 untouched, dependent tables 0/0/0, 0 orphan FKs. Conservative NULL policy preserved: service_type/status/location/time_slot/tip/rent_paid/net all explicit NULL, notes carry the Codex description + `[Phase 3.5.1 backfill 2026-05-17]` tag. SQL file restored to default ROLLBACK. Execution report at `_reports/2026-05-17-phase-3.5.1-execution-report.md`. Phase 3.5.1's only remaining candidate (Kiwi 2024-07-??) stays pending Russell's own card readback. |
| 2026-05-17 | CC → Russell | **v2 M3 wedge — fast-lookup search, first slice.** Rebuilt the Call/Text → Identify → Book search in `v2/` (PRD §1.1, M3). One box matches owner first/last name, phone, and pet name with exact/prefix/substring/fuzzy (Levenshtein) ranking; results are household cards (owner, phone, pets + allergy flags, last visit, usual service/price derived on read); a shared pet name ("Bella" ×2) is disambiguated by emphasizing the matched pet. New pure module `lib/search.ts` + `derive.ts` additions (`usualService` mode, `usualPrice` median) TDD'd with Vitest — the venture's first test harness — 39 tests. UI rewrite: `page.tsx`, `ClientSearch.tsx`, new `HouseholdCard.tsx`; `fixtures.ts` gains 2 synthetic clients / 3 pets for the two-Bella demo. typecheck + lint + build + 39 tests green; a curl check confirms the proxy still gates every route after the rewrite. Interactive authenticated search click-through handed to Russell (needs a browser + a Supabase Auth account). Read-only — no booking writes (deferred to the M2 write path), no SQL, no RLS/schema change, no SMS; fixtures stay default; Ship 2.2a auth untouched. Next: Russell browser-tests login + search; then the booking increment of M3, or Ship 2.2b prereqs. |
| 2026-05-17 | Cowork → Russell | **Phase 3.5 appointment backfills COMMITTED.** Fresh pre-commit backup at `_private/backups/2026-05-17-phase-3.5-precommit/` (six tables, MANIFEST + SHA-256 sealed) before flipping `ROLLBACK;` → `COMMIT;`. Single INSERT inserted 5 rows (Whiskey×3 + Kiwi×2). All 9 pre-write gates (G1-G9) and 7 post-write invariants (I1-I7) passed. Final summary: `clients_delta=0`, `pets_delta=0`, `appointments_delta=+5`. Independent post-COMMIT verification at 14:44:16 UTC: 137/188/735, Landry 4 untouched, Korrie holdout 1 untouched, dependent tables 0/0/0, 0 orphan FKs. No Supabase errors or notices. SQL file restored to default ROLLBACK. Conservative NULL policy applied: service_type/status/location/time_slot/tip/rent_paid/net all explicitly NULL (Codex silent), notes carry the Codex grooming description + `[Phase 3.5 backfill 2026-05-17]` tag. The first rollback attempt earlier in the day caught a v2 design-lock §6.1 schema gap via G9 — appointments table has 14 columns, not the 7 the spec listed. The spec was corrected in the same loop. Execution report at `_reports/2026-05-17-phase-3.5-execution-report.md`. B5 (Russell Cole / Kiwi import gap) is now partially closed (2 of 4 card-back visits backfilled). |
| 2026-05-17 | CC → Russell | **v2 Ship 2.2a — real Supabase Auth.** Replaced the placeholder cookie gate with real Supabase Auth in `v2/` — app-side only, scoped by Russell as no migrations / no RLS change. New: `lib/supabase/{env,server,client,proxy}.ts` + `components/LoginForm.tsx`. Rewritten: `proxy.ts`, `lib/actions/auth.ts`, the login + settings pages. Deleted the placeholder `lib/session.ts`. The proxy gates every app route via the Supabase SSR session and refreshes it; `signIn`/`signOut` use `signInWithPassword` / `auth.signOut()`. typecheck + lint + build green; dev-server checks confirm unauthenticated app routes 307 → `/login`, `/login` renders the real form, and the Supabase auth endpoint resolves. Interactive sign-in → app → sign-out click-through handed to Russell (needs a browser + a Supabase Auth account; populate `v2/.env.local` from `.env.local.example` first). No DB migration, no SQL, no RLS/schema change, no SMS, no live writes; data stays read-only, fixtures stay default. The live-data path still uses the standalone anon client — must switch to the auth client before the 2.2b cutover. Next: Russell browser-tests login, then Ship 2.2b prereqs or the M3 fast-lookup build. |
| 2026-05-17 | Cowork → Russell | **Phase 3 INSERTs COMMITTED.** Fresh pre-commit backup at `_private/backups/2026-05-17-phase-3-precommit/` (six tables, MANIFEST + SHA-256 sealed) before flipping `ROLLBACK;` → `COMMIT;`. Section 1-7 executed once via Supabase MCP. All 7 pre-write gates (G1, G2, G3, G4, G5a, G5b, G6) passed; 6 client INSERTs + 7 pet INSERTs ran; all 7 post-write invariants (I1-I7) passed. Final summary: `clients_delta=+6`, `pets_delta=+7`, `appointments_delta=0`. Independent post-COMMIT verification at 00:53:56 UTC: 137 clients, 188 pets, 730 appointments, Landry 4 (untouched), Korrie holdout 1 (untouched at 647-300-7952), dependent tables 0/0/0, 0 orphan appointment FKs. No Supabase errors or notices. SQL file restored to default ROLLBACK. New rows are Mary Anca/Whiskey, Nancy Cauchi/Ruby, Gardy/Coco, Ashley Nichols/Kahlúa, Mary Nichols/Merlyn+Vader, Christina Kitchen/Winston. Execution report at `_reports/2026-05-17-phase-3-execution-report.md` (includes all 6 client UUIDs + 7 pet UUIDs). Korrie Silver / Gavi held out per G5 rollback-test catch — separate UPDATE patch needed once Sam clarifies phone format, Gavi dedup, and allergy detail. |
| 2026-05-16 | CC → Russell/Sam | **Sam Scheduling OS PRD — immediate wedge tightened.** Russell relayed a follow-up Russell/Sam product correction: keep v2 as the scheduling source of truth, but make the *immediate* build wedge fast client/pet lookup and confident booking — **Call/Text → Identify → Book** — not scheduling automation. Samantha wants to type a name/phone/pet name/partial clue and instantly pull the right household (which "Bella," owner, phone, household pets, last visit, usual service, usual price/duration, allergies/notes) to book confidently. Planning docs updated only: PRD gains §1.1 (the wedge), re-sequenced so M3 = fast lookup / disambiguation / confident booking and M4 = the schedule-of-record view; M5 guided warnings stay but are explicitly secondary/advisory; §7 Flow 2 rewritten; two new §10 acceptance criteria (10-second identification; book without re-asking known details). Scope change (source of truth + wedge) logged in `docs/DECISIONS.md` as a combined 2026-05-16 entry. Answers artifact gains a follow-up-correction section; KoyaOS dogfood doc gains REQ-26 (separate the job-to-be-done from adjacent complexity). No code, no SQL, no Supabase mutation. Committed: PRD, answers artifact, KoyaOS doc, docs/DECISIONS.md, HANDOFF. Next: Russell reviews the wedge-tightened PRD and starts the build (M1–M3 unblocked); clear R1–R4 with Sam alongside. |
| 2026-05-16 | CC → Russell/Sam | **Sam Scheduling OS PRD updated from Samantha's Group A answers.** All six Group A questions answered (relayed by Russell). Q-A4, load-bearing: Tidy Tails should be the scheduling source of truth for upcoming appointments, not a mirror — this inverts the prior "scheduling-aware cockpit" stance. PRD rewritten end-to-end: §1 states the scope change plainly (deliberate, beyond the conservative "today list" the locked recon imagined — a `docs/DECISIONS.md` entry recommended); §4 redefines v2 as a **phased guided scheduler** (M3–M4 schedule spine, M5 soft warnings, M6 reminders) under a "do not over-automate Sam's judgment" principle — warn and explain, never silently block except genuine impossibilities; the graduation gate is retired. §3 carries Sam's recurring weekly pattern and contextual capacity rules; §6.1 defines a scheduling-rules model (recurring availability with alternation + Nth-weekday-of-month, flexible location model so Ren's is data not schema, soft capacity references, service duration estimate + per-dog override, temperament override, can-be-out-of-crate flag, hard-vs-soft warnings); §8 reframed from "fix bad records" to "Keeping the digital book current." Residuals R1–R4 remain, non-blocking. Created the answers artifact `_reports/2026-05-16-sam-scheduling-answers.md`; KoyaOS dogfood doc gained REQ-24 (pair a deferral gate with asking the operator) and REQ-25 (elicit operator-facing language, don't impose it). Committed: the PRD, the answers artifact, the KoyaOS doc, this HANDOFF. No code, no SQL, no Supabase mutation. Next: Russell/Sam clear R1–R4 and log the DECISIONS.md scope-change entry; the build guardrail is lifted — M1 (operator auth) is buildable now. |
| 2026-05-16 | CC → Russell/Sam | **Sam Scheduling OS PRD checkpoint pass.** Added §8 to the PRD — "Sam can fix the book herself," data correction as a first-class workflow (inline edit from every detail surface, add-missing client/pet/appointment, archive-not-delete, tiered confirmations, audit/edit history, "unknown for now" handling, single/future-multi-groomer role-auth, acceptance criteria). Added §12 PRD review notes; renumbered the old §8/§9/§10 to §9/§10/§11. Created a plain-language Samantha-facing Group A question sheet at `_reports/2026-05-16-sam-scheduling-questions.md`. KoyaOS dogfood doc now carries REQ-23 (ground gap analysis in the live system, not doc summaries). Committed the PRD, the KoyaOS dogfood doc, the question sheet, and this HANDOFF. No code, no SQL, no Supabase mutation. Next: Russell/Sam answer Group A; build stays at auth + read-only polish until the schedule source-of-truth question (Q-A4) is answered. |
| 2026-05-16 | CC → Russell | **Sam Scheduling OS PRD written.** `_reports/2026-05-16-sam-scheduling-os-prd.md` — product definition for the v2 grooming cockpit. Resolved the "Scheduling OS" kickoff vs locked-spec conflict via Russell's call: v2 is a scheduling-*aware* cockpit, not a scheduler (Option A), with a "Not Yet: Full Scheduler" section and a graduation gate (criteria + review cadence + owner). Includes gap analysis, a 9-part scheduling domain model (every parameter tagged KNOWN or UNKNOWN — no fabricated operator data), a Group A / Group B question batch for Sam, schema implications (prose, no migrations), 6 UX flows, a 7-milestone plan, and acceptance criteria with a lived-adoption bar. KoyaOS dogfood REQ-19–22 at `_reports/2026-05-16-koyaos-dogfood-sam-scheduling-os-prd.md`. PRD/spec only — no code, no SQL, no Supabase mutation, nothing committed. |
| 2026-05-16 | Cowork → Russell | **Phase 2 dedup COMMITTED.** Fresh pre-commit backup taken at `_private/backups/2026-05-16-phase-2-precommit/` (six tables, manifest + SHA-256 sealed) before flipping `ROLLBACK;` → `COMMIT;`. Section 2 executed once via Supabase MCP. All 8 pre-write gates G1-G8 passed; both UPDATEs and both DELETEs ran; all 7 post-write invariants I1-I7 passed; final summary row: `clients_delta=-137`, `pets_delta=-171`, `appointments_delta=0`, `ghosts_deleted=137`, `canonicals_kept=113`. Independent post-COMMIT live verification at 19:18:07 UTC: 131 clients, 181 pets, 730 appointments, 4 Landry/Laundry rows, 0 booking_requests/client_accounts/automations_log, 0 remaining duplicate phone groups outside Landry, 0 orphan appointment FKs. No Supabase errors or notices. SQL file restored to default ROLLBACK. Execution report at `_reports/2026-05-16-phase-2-execution-report.md`. |
| 2026-05-15 | Cowork → Russell | Phase 2 SQL + notes re-baselined to post-Phase-1 state per Codex verifier sign-off. The 2026-05-15 read-only pre-flight (Section 0.1–0.6 + Section 1) ran against the live DB with zero writes; outputs captured verbatim in `_reports/2026-05-15-phase-2-pre-flight-output.md`. G2 and G4 originally hardcoded the pre-Phase-1 numbers (103+12=115 / 250/114/136); Phase 1B's Backway phone normalization collapsed two 2-row clusters into one 4-row cluster, shifting the inventory to (101+13=114) and the working set to (250/113/137). The gates correctly fail-loud caught the drift. Updated: G2 → (101, 13, 0, 114). G4 → (250, 113, 137). All explanatory comments, predicted deltas, and run-order playbook rewritten. New predicted `clients_delta == -137`, clients 268 → 131. Landry exclusion preserved, G8 preserved, default ROLLBACK preserved. Zero DB writes performed. |
| 2026-05-15 | CC → Russell | Committed the v2 Ship 2.1 scaffold (`2e9a5cb`, 57 files; `docs/home.html` excluded — unrelated v1 color change). Wrote the Ship 2.2 auth + RLS plan (`_reports/2026-05-15-v2-ship-2.2-auth-rls-plan.md`) — flag-day cutover, no anon bridge; covers app-side Supabase Auth wiring, the RLS migration as draft SQL, rollback, backup requirement, verifier checklist. KoyaOS dogfood notes REQ-15–18 at `_reports/2026-05-15-koyaos-dogfood-v2-ship-2.2-planning.md`. Two decisions logged in `docs/DECISIONS.md`. Planning only — no SQL run, no migration applied, no backup taken. |
| 2026-05-15 | Cowork → Russell | Phase 2 dedup SQL hardened per Russell's review. Two files: `_reports/2026-05-15-phase-2-dedup.sql` (set-based, temp-table-driven, 8 pre-write gates G1-G8 + 7 post-write invariants I1-I7, defaults to ROLLBACK) and `_reports/2026-05-15-phase-2-dedup-notes.md` (verifier reading aid + run playbook). Pre-write gates: G1 baseline counts (268/352/730), G2 dup inventory (103+12=115), G3 Landry exactly 4 rows, G4 working set (250 ranked / 114 canonical / 136 ghosts), G5-G7 set integrity, **G8 dependent tables (`booking_requests`, `client_accounts`, `automations_log`) all 0 rows — fails if v1 has begun writing to a surface this script does not model in its FK blast radius.** Section 0.5 schema audit for pets/appointments `updated_at`, Section 0.6 audit for the three dependent tables. Final summary includes explicit `clients_delta` / `pets_delta` / `appointments_delta` columns. Landry/Laundry blocker enforced at 5 layers. Zero DB writes performed. |
| 2026-05-15 | CC → Russell | Built the v2 Ship 2.1 read-only scaffold in `v2/` — Next.js 16 mobile-first PWA grooming cockpit. Search, client/pet detail, allergy-first safety cards, appointment history, lapsed clients, vaccination status, revenue. Groom-logging + SMS UIs built but write-disabled. Build clean, browser-verified, no console errors. Zero live DB writes, no SQL, no SMS, no v1 changes, no deploy. Repo strategy + scaffold authorization logged in `docs/DECISIONS.md`. **(Scaffold later committed as `2e9a5cb` — see the 2026-05-15 CC commit entry above.)** |
| 2026-05-15 | Codex → Russell/Cowork/CC | Pawfinity logged-in recon completed read-only. No live records mutated, exported, messaged, scheduled, charged, or deleted. Created `_reports/2026-05-15-pawfinity-logged-in-recon.md` and `_reports/2026-05-15-pawfinity-v2-implications.md`. Main product decision: Tidy v2 should be a mobile-first Samantha grooming cockpit, not a Pawfinity clone. |
| 2026-05-15 | Cowork → Russell | Phase 1 SQL executed and verified. 29 UPDATEs, 18 client rows + 11 pet rows. All spot-checks pass. Appointments unchanged at 730. Fresh backup at venture-ops/backups/tidy-tails/2026-05-15/. Execution report at _reports/2026-05-15-phase-1-execution-report.md. State docs updated. v2 design-lock prep doc written. Next: Phase 2 dedup SQL. |
| 2026-05-13 late EOD | Cowork → Sam | Review pipeline shipped end-to-end. Page live at symphonious-starlight-2a7ecc.netlify.app, Resend wired, Sam answered 22 distinct Qs (30 raw rows). Caught a UX bug mid-session (notes were gated behind a radio selection); fixed by adding "Other / write below" + notes-on-every-question + note-only-Next-enable. Sam paused around Q23/49; will continue tomorrow. |
| 2026-05-13 EOD | Cowork → Russell | 79 contact cards processed across 8 batches. Workstream A closed earlier; Workstream B paused per Russell's "last batch for now" signal. Workstream C still queued (Pawfinity logged-in recon). Open questions log carries ~215 Russell-only decisions. Zero DB writes executed by Cowork on customer data. |
| 2026-05-13 | Cowork → Cowork (workstream pivot) | Workstream A closed; Workstream B opened. Koya feedback doc relayed (12/12 triaged into KoyaOS; 3 became Phase 5B ships). HANDOFF refreshed mid-session before first card batch landed. |
| 2026-04-21 | Cowork → Russell | Migrated from `russellcolevop/tidy-tails` into the `russell-labs` org. Studio dashboard reflects it. Russell notifies Samantha + opens the Tidy Tails Cowork project for the v2 enrichment and Supabase conversion. |
